#version 450 core

in vec3 vertexColor;
in vec2 vertexUV;

uniform vec4 materialColor;

out vec4 oColor;

void main() {
    oColor      = vec4(vertexColor, 1.0) * materialColor;
    oColor.rgb *= oColor.a;
}